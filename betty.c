#include <stdio.h>

/*
 * void_sum - Entry point of the function sum
 * @a: firt number
 * @b: second number
 * Return: sum
 */

void_sum(int a, int b)
{
	printf("This is to pass betty checks");
	return (a + b);
}
